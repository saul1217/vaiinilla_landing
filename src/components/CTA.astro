<section class="cta" id="cta">
  <div class="container">
    <div class="cta__card" data-reveal>
      <img class="cta__logo" src="/brand/vaiinilla-logo-splash-light.png" alt="Vaiinilla" width="1448" height="1086" loading="lazy" />
      <div class="cta__copy"><p class="eyebrow">Cuando todo está en su lugar</p><h2>Tu siguiente pedido<br /><span>empieza aquí.</span></h2><p>Descubre cómo se siente una cafetería que fluye.</p><a class="btn btn--dark" href="https://app.vaiinilla.app" target="_blank" rel="noreferrer">Abrir Vaiinilla <span aria-hidden="true">↗</span></a></div>
    </div>
  </div>
</section>

<footer class="footer">
  <div class="container footer__inner">
    <a class="footer__logo" href="#top" aria-label="Vaiinilla — inicio"><img src="/brand/vaiinilla-mark.webp" alt="" width="42" height="42" /><span translate="no">Vaiinilla</span></a>
    <p class="footer__tag">Menú, pedidos y operación para cafeterías escolares.</p>
    <a class="footer__app" href="https://app.vaiinilla.app" target="_blank" rel="noreferrer">app.vaiinilla.app ↗</a>
  </div>
</footer>

<style>
  .cta { padding: 110px 0 72px; } .cta__card { position: relative; min-height: 420px; overflow: hidden; display: flex; align-items: center; padding: clamp(34px, 7vw, 80px); border-radius: 30px; background: var(--lime); } .cta__card::after { content: ''; position: absolute; right: -90px; bottom: -140px; width: 440px; height: 440px; border: 58px solid rgba(251,249,242,.34); border-radius: 50%; }
  .cta__logo { position: absolute; right: 6%; bottom: -16%; width: min(390px, 43%); height: auto; opacity: .88; transform: rotate(8deg); } .cta__copy { position: relative; z-index: 1; } .cta__copy .eyebrow { color: rgba(22,21,15,.65); } .cta__copy h2 { margin: 18px 0 16px; font-size: clamp(2.6rem, 6vw, 5.2rem); line-height: .9; } .cta__copy h2 span { color: var(--white); } .cta__copy > p:last-of-type { margin-bottom: 28px; color: var(--ink-soft); }
  .footer { padding: 0 0 42px; } .footer__inner { display: flex; align-items: center; gap: 24px; justify-content: space-between; padding-top: 26px; border-top: 1px solid var(--line); } .footer__logo { display: inline-flex; align-items: center; gap: 10px; font-weight: 800; } .footer__logo img { width: 42px; height: 42px; object-fit: contain; } .footer__tag, .footer__app { color: var(--muted); font-size: .82rem; } .footer__app { transition: color 180ms ease; } .footer__app:hover { color: var(--ink); }
  @media (max-width: 650px) { .cta { padding: 78px 0 54px; } .cta__card { min-height: 520px; align-items: flex-start; } .cta__logo { right: -8%; bottom: -2%; width: 78%; opacity: .5; } .footer__inner { align-items: flex-start; flex-direction: column; gap: 12px; } }
</style>
