## Development

When starting the dev server, use:

```
npm run dev
```

This repo is Vite + React (no Astro). Marketing and buyer live in the same SPA.

The establishment panel is a different app (`vaiinilla-web` on `app.vaiinilla.app`).
